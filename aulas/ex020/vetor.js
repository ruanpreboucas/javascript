let valores = [8, 1, 7, 4, 3, 54, 29, -1]


/*for (let c=0; c <=4; c++){
    console.log(valores[c])
}*/ // valores do vetor até a posição 5

console.log(valores)
 
/*for (let c=0; c < valores.length; c++){
    console.log(`o item da posição ${c} tem valor ${valores[c]}`)
}
*/

/*for (c in valores){
    console.log(`o item da posição ${c} tem valor ${valores[c]}`)
}
*/

let c = valores.indexOf(9)
console.log(`o valor está na posição ${c}`)
